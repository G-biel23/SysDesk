<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de fundo da mensagem -->
    <solid android:color="#D1F5FE"/>

    <!-- Bordas arredondadas -->
    <corners android:radius="16dp"/>

    <!-- Margem interna se quiser -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
</shape>
