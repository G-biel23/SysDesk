<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SysDesk" >

        <!-- Tela de login -->
        <activity
            android:name=".LoginForm"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de cadastro -->
        <activity
            android:name=".CadastroForm"
            android:exported="true" />

        <!-- Tela Home de Cliente -->
        <activity
            android:name=".ClienteHomeForm"
            android:exported="true" />

        <!-- Tela Home de Admin -->
        <activity
            android:name=".AdminHomeForm"
            android:exported="true" />

        <!-- Tela Home de Técnico -->
        <activity
            android:name=".TecnicoHomeForm"
            android:exported="true" />


    </application>

</manifest>
